{
  "name": "Cardersation",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "install-all": "npm install && cd client/client && npm install && cd ../../server && npm install",
    "build": "cd client/client && npm install && npm run build && cd ../../server && npm install",
    "start": "cd server && node server.js",
    "dev": "concurrently \"cd server && npm run dev\" \"cd client/client && npm run dev\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "eslint": "^9.26.0"
  }
}